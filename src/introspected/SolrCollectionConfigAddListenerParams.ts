/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: add-listener
 */

export interface SolrCollectionConfigAddListenerParams {
  /**
   * Name by which it is added, so that it can be updated by name
   */
  name: string;
}
