/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: delete-requesthandler
 */

/**
 * Deletes one or more request handlers, using the name given when the request handler was created. Define more than one request handler by separating the list of names with commas.
 */
export type SolrCollectionConfigDeleteRequesthandlerParams = string[];
