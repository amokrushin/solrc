/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: delete-queryconverter
 */

/**
 * Deletes one or more query converters, using the name given when the query converter was created. Define more than one query converters by separating the list of names with commas.
 */
export type SolrCollectionConfigDeleteQueryconverterParams = string[];
