/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: delete-searchcomponent
 */

/**
 * Deletes one or more search components, using the name given when the search component was created. Define more than one search component by separating the list of names with commas.
 */
export type SolrCollectionConfigDeleteSearchcomponentParams = string[];
