/*
 * Introspected endpoint: /collections/{collection}/schema
 * Command: delete-field-type
 */

/**
 * Deletes a field type from the schema.
 */
export interface SolrCollectionSchemaDeleteFieldTypeParams {
  /**
   * The name of the field type to delete.
   */
  name: string;
}
