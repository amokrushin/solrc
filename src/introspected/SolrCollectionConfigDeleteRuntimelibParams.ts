/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: delete-runtimelib
 */

/**
 * Deletes one or more runtime libraries (runtimeLibs), using the name given when the runtimeLib was created. Define more than one runtimeLibs by separating the list of names with commas.
 * @version Solr 8.x
 */
export type SolrCollectionConfigDeleteRuntimelibParams = string[];
