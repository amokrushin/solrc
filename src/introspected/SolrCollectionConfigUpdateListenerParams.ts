/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: update-listener
 */

export interface SolrCollectionConfigUpdateListenerParams {
  /**
   * Name by which it is added
   */
  name: string;
}
