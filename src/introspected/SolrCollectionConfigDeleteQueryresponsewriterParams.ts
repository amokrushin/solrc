/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: delete-queryresponsewriter
 */

/**
 * Deletes one or more query response writers, using the name given when the response writer was created. Define more than one response writer by separating the list of names with commas.
 */
export type SolrCollectionConfigDeleteQueryresponsewriterParams = string[];
