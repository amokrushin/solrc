/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: set-property:
 */

/**
 * Sets one or more of several pre-defined properties. These properties set cache sizes and classes, commit rules, JMX settings, and request dispatcher settings. See the documentation for the list of properties that are supported. If a property is set that already exists, it will be overwritten.
 */
export interface SolrCollectionConfigSetPropertyParams {}
