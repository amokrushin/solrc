/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: delete-queryparser
 */

/**
 * Deletes one or more query parsers, using the name given when the query parser was created. Define more than one query parser by separating the list of names with commas.
 */
export type SolrCollectionConfigDeleteQueryparserParams = string[];
