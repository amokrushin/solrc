/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: delete-listener
 */

/**
 * Deletes one or more listeners, using the name given when the listener was created. Define more than one listener by separating the list of names with commas.
 */
export type SolrCollectionConfigDeleteListenerParams = string[];
