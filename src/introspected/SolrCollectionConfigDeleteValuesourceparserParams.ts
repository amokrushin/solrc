/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: delete-valuesourceparser
 */

/**
 * Deletes one or more ValueSourceParsers, using the name given when the ValueSourceParser was created. Define more than one ValueSourceParsers by separating the list of names with commas.
 */
export type SolrCollectionConfigDeleteValuesourceparserParams = string[];
