/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: delete-initparams
 */

/**
 * Deletes one or more init params, using the name given when the init param set was created. Define more than one init params by separating the list of names with commas.
 */
export type SolrCollectionConfigDeleteInitparamsParams = string[];
