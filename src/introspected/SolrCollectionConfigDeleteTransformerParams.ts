/*
 * Introspected endpoint: /collections/{collection}/config
 * Command: delete-transformer
 */

/**
 * Deletes one or more document transformers, using the name given when the document transformer was created. Define more than one document transformers by separating the list of names with commas.
 */
export type SolrCollectionConfigDeleteTransformerParams = string[];
