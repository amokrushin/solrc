/*
 * Introspected endpoint: /collections/{collection}/config/params
 * Command: set:
 */

/**
 * Add or overwrite one or more paramsets. Each paramset definition includes a paramset name, followed by key-value pairs of the parameter and value to be set.
 */
export interface SolrCollectionConfigParamsSetParams {}
